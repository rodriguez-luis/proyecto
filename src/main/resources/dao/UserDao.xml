<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.UserDao">
    <select id="findByUserId" resultType="bo.ucb.edu.ingsoft.model.User">
        SELECT
        *
        FROM user
        WHERE
            id_user = 0
    </select>
    <insert id="create" parameterType="bo.ucb.edu.ingsoft.model.User">
        INSERT INTO user VALUES (
            null, #{personId}, #{companyId}, 1, #{username}, #{password}, 0, 0,#{txId}, #{txHost}, #{txUserId}, #{txDate}
        )
    </insert>
    <select id="getLastInsertId" resultType="java.lang.Integer">
        SELECT LAST_INSERT_ID()
    </select>
</mapper>
